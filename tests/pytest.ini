[pytest]
markers =
    asyncio: mark test as an async test
    sync: mark test as a sync test

testpaths =
    tests/sync
    tests/async

python_files =
    test_*.py
    *_test.py

addopts =
    -v
    --tb=short
    --strict-markers

asyncio_mode = strict 